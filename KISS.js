function isKiss( words ){
let split = words.split(" ");
let totalCount = split.length;
  
  for(let i = 0; i<split.length;i++){
    if(split[i].length > totalCount){
 
return "Keep It Simple Stupid"
    }
    
  }
  
  return "Good work Joe!"

  
  

}
